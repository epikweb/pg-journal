{
  "name": "@pg-journal/event-store",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@pg-journal/postgres-client": "^1.0.0",
    "dotenv": "^10.0.0"
  },
  "dependencies": {
    "@pg-journal/postgres-client": "^1.0.0",
    "chai": "^4.3.4",
    "uuid": "^8.3.2"
  },
  "scripts": {
    "test:setup": "docker-compose up -d && npx wait-on -d 1000 tcp:30000 && node test/setup.js",
    "test": "npx mocha {src,test}/**/*.spec.js --exit"
  },
  "peerDependencies": {
    "@pg-journal/postgres-client": "^1.0.0"
  }
}
